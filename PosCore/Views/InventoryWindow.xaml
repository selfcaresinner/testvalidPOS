<Window x:Class="PosCore.Views.InventoryWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:PosCore.Views"
        mc:Ignorable="d"
        Title="Control de Inventario - POS Express" Height="650" Width="1000" 
        WindowStartupLocation="CenterScreen" Background="#F3F4F6">
    <Window.Resources>
        <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter" />
    </Window.Resources>
    
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        
        
                <Grid Margin="0,0,0,20">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>
                    <TextBlock Text="Control de Inventario" FontSize="28" FontWeight="Bold" Foreground="#111827" />
                    <StackPanel Grid.Column="1" Orientation="Horizontal">
                        <Button Content="📥 Importar (CSV)" Command="{Binding ImportProductsCommand}" Height="35" Width="130" Background="#F59E0B" Foreground="White" FontWeight="Bold" BorderThickness="0" Margin="0,0,10,0" Cursor="Hand"/>
                        <Button Content="📤 Exportar (PDF/Excel)" Command="{Binding ExportProductsCommand}" Height="35" Width="130" Background="#17A2B8" Foreground="White" FontWeight="Bold" BorderThickness="0" Cursor="Hand"/>
                    </StackPanel>
                </Grid>

        
        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="2.5*"/>
                <ColumnDefinition Width="1.5*"/>
            </Grid.ColumnDefinitions>
            
            <!-- Lista de Productos -->
            <Grid Grid.Column="0" Margin="0,0,20,0">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>
                
                
                <StackPanel Orientation="Horizontal" Margin="0,0,0,15">
                    <TextBox Text="{Binding SearchQuery, UpdateSourceTrigger=PropertyChanged}" Width="200" Height="40" Margin="0,0,15,0" Padding="10,0" FontSize="14" BorderBrush="#D1D5DB" BorderThickness="1" Background="White" VerticalContentAlignment="Center" ToolTip="Buscar por nombre o código"/>
                    <Button Content="➕ Agregar Nuevo" Command="{Binding AddProductCommand}" Width="140" Height="40" Background="#10B981" Foreground="White" FontWeight="Bold" BorderThickness="0" Margin="0,0,10,0" Cursor="Hand"/>
                    <Button Content="✏️ Editar" Command="{Binding EditProductCommand}" Width="110" Height="40" Background="#3B82F6" Foreground="White" FontWeight="Bold" BorderThickness="0" Margin="0,0,10,0" Cursor="Hand"/>
                    <Button Content="🗑️ Eliminar" Command="{Binding DeleteProductCommand}" Width="110" Height="40" Background="#EF4444" Foreground="White" FontWeight="Bold" BorderThickness="0" Cursor="Hand"/>
                </StackPanel>
                
                <Border Grid.Row="1" Background="White" BorderBrush="#E5E7EB" BorderThickness="1" CornerRadius="8">
                    <DataGrid ItemsSource="{Binding Products}" SelectedItem="{Binding SelectedProduct}" AutoGenerateColumns="False" CanUserAddRows="False" IsReadOnly="True" 
                              HeadersVisibility="Column" SelectionMode="Single" Background="Transparent" BorderThickness="0" GridLinesVisibility="Horizontal" HorizontalGridLinesBrush="#F3F4F6" RowHeight="40" AlternatingRowBackground="#F9FAFB">
                        <DataGrid.Resources>
                            <Style TargetType="DataGridColumnHeader">
                                <Setter Property="Background" Value="#F9FAFB"/>
                                <Setter Property="Foreground" Value="#6B7280"/>
                                <Setter Property="FontWeight" Value="SemiBold"/>
                                <Setter Property="Padding" Value="10,12"/>
                            </Style>
                        </DataGrid.Resources>
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="Código" Binding="{Binding Barcode}" Width="120"/>
                            <DataGridTextColumn Header="Nombre" Binding="{Binding Name}" Width="*"/>
                            <DataGridTextColumn Header="Precio" Binding="{Binding Price, StringFormat=C}" Width="100"/>
                            <DataGridTextColumn Header="Stock" Binding="{Binding StockQuantity}" Width="80"/>
                            <DataGridTextColumn Header="Min." Binding="{Binding MinStockThreshold}" Width="80"/>
                        </DataGrid.Columns>
                    </DataGrid>
                </Border>
            </Grid>
            
            <!-- Formulario de Edición -->
            <Border Grid.Column="1" Background="White" Padding="25" CornerRadius="8" BorderBrush="#E5E7EB" BorderThickness="1" Visibility="{Binding IsEditing, Converter={StaticResource BooleanToVisibilityConverter}}">
                <StackPanel>
                    <TextBlock Text="Detalles del Producto" FontSize="20" FontWeight="Bold" Foreground="#1F2937" Margin="0,0,0,20"/>
                    
                    <TextBlock Text="Código de Barras" FontSize="14" FontWeight="SemiBold" Foreground="#4B5563" Margin="0,0,0,5"/>
                    
                    <Grid Margin="0,0,0,15">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <TextBox Text="{Binding EditingProduct.Barcode, UpdateSourceTrigger=PropertyChanged}" Height="40" Margin="0,0,10,0" Padding="10,0" FontSize="14" BorderBrush="#D1D5DB" BorderThickness="1" Background="#F9FAFB" VerticalContentAlignment="Center"/>
                        <Button Grid.Column="1" Content="Generar" Command="{Binding GenerateBarcodeCommand}" Width="80" Height="40" Background="#3B82F6" Foreground="White" FontWeight="Bold" BorderThickness="0" Cursor="Hand" ToolTip="Generar Código Automático"/>
                    </Grid>

                    
                    <TextBlock Text="Nombre del Producto" FontSize="14" FontWeight="SemiBold" Foreground="#4B5563" Margin="0,0,0,5"/>
                    <TextBox Text="{Binding EditingProduct.Name, UpdateSourceTrigger=PropertyChanged}" Height="40" Margin="0,0,0,15" Padding="10,0" FontSize="14" BorderBrush="#D1D5DB" BorderThickness="1" Background="#F9FAFB" VerticalContentAlignment="Center"/>
                    
                    <TextBlock Text="Precio (Venta)" FontSize="14" FontWeight="SemiBold" Foreground="#4B5563" Margin="0,0,0,5"/>
                    <TextBox Text="{Binding EditingProduct.Price, UpdateSourceTrigger=PropertyChanged}" Height="40" Margin="0,0,0,15" Padding="10,0" FontSize="14" BorderBrush="#D1D5DB" BorderThickness="1" Background="#F9FAFB" VerticalContentAlignment="Center"/>
                    
                    <TextBlock Text="Stock Actual" FontSize="14" FontWeight="SemiBold" Foreground="#4B5563" Margin="0,0,0,5"/>
                    <TextBox Text="{Binding EditingProduct.StockQuantity, UpdateSourceTrigger=PropertyChanged}" Height="40" Margin="0,0,0,15" Padding="10,0" FontSize="14" BorderBrush="#D1D5DB" BorderThickness="1" Background="#F9FAFB" VerticalContentAlignment="Center"/>
                    
                    <TextBlock Text="Alerta Stock Mínimo" FontSize="14" FontWeight="SemiBold" Foreground="#4B5563" Margin="0,0,0,5"/>
                    <TextBox Text="{Binding EditingProduct.MinStockThreshold, UpdateSourceTrigger=PropertyChanged}" Height="40" Margin="0,0,0,25" Padding="10,0" FontSize="14" BorderBrush="#D1D5DB" BorderThickness="1" Background="#F9FAFB" VerticalContentAlignment="Center"/>
                    
                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                        <Button Content="Cancelar" Command="{Binding CancelEditCommand}" Width="100" Height="40" Background="#E5E7EB" Foreground="#374151" FontWeight="Bold" BorderThickness="0" Margin="0,0,10,0" Cursor="Hand"/>
                        <Button Content="Guardar" Command="{Binding SaveProductCommand}" Width="120" Height="40" Background="#10B981" Foreground="White" FontWeight="Bold" BorderThickness="0" Cursor="Hand"/>
                    </StackPanel>
                </StackPanel>
            </Border>
        </Grid>
    </Grid>
</Window>
